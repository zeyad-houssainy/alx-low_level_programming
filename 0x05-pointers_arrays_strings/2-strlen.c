#include "main.h"
/**
*_strlen - returns the length of a string.
*
*@s: number 1
*Return:lnegth
*/

int _strlen(char *s)
{
	int count;
	for (count = 0; *(s + count) != '\0'; count++)
	{
	}
	return (count);
}
